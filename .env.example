# Copy to .env and fill in.
# The client ID of a Spotify app from https://developer.spotify.com/dashboard
# This is a public identifier, not a secret — the app uses PKCE and never has
# a client secret.
VITE_SPOTIFY_CLIENT_ID=
